/* $(document).ready(function () {
    var texto = $('#meu-texto').text();

    function escreverTexto(i) {
        $('.maquina-escrever').text(texto.slice(0, i));
        if (i < texto.length) {
            setTimeout(function () {
                escreverTexto(i + 1);
            }, 100); 
        }
    }

    escreverTexto(0);
}); */